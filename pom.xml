<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.0</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>org.lifxue</groupId>
    <artifactId>WuZhu</artifactId>
    <version>1.0</version>
    <name>WuZhu</name>
    <description>
        WuZhu is a cryptocurrency tools for records transaction information and analyzes crypto data.
    </description>
    <properties>
        <java.version>21</java.version>
        <javafx.version>21.0.2</javafx.version>
        <workbenchfx.version>11.3.1</workbenchfx.version>
        <fx.weaver>2.0.1</fx.weaver>
        <mybatisplus.version>3.5.3.1</mybatisplus.version>
        <mapstructVersion>1.6.2</mapstructVersion>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- https://mvnrepository.com/artifact/org.springframework.cloud/spring-cloud-dependencies -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>2023.0.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- 统一 JavaFX 版本，防止模块版本冲突 -->
            <dependency>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-base</artifactId>
                <version>${javafx.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-graphics</artifactId>
                <version>${javafx.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-controls</artifactId>
                <version>${javafx.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-fxml</artifactId>
                <version>${javafx.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-web</artifactId>
                <version>${javafx.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-media</artifactId>
                <version>${javafx.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-swing</artifactId>
                <version>${javafx.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- springboot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!--http-->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.openfeign</groupId>
            <artifactId>feign-okhttp</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.httpcomponents</groupId>
            <artifactId>httpclient</artifactId>
            <version>4.5.14</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <!--database-->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>2.2.224</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
      <!--  <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-boot-starter</artifactId>
            <version>${mybatisplus.version}</version>
        </dependency>
         <dependency>
             <groupId>com.baomidou</groupId>
             <artifactId>mybatis-plus-generator</artifactId>
             <version>${mybatisplus.version}</version>
         </dependency>-->

        <!--javafx-->
        <!-- 显式声明所有 JavaFX 模块，确保版本一致 -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-base</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-media</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-swing</artifactId>
        </dependency>
        <dependency>
            <groupId>net.rgielen</groupId>
            <artifactId>javafx-weaver-spring-boot-starter</artifactId>
            <version>${fx.weaver}</version>
        </dependency>
        <dependency>
            <groupId>com.dlsc.workbenchfx</groupId>
            <artifactId>workbenchfx-core</artifactId>
            <version>${workbenchfx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.fxmisc.richtext</groupId>
            <artifactId>richtextfx</artifactId>
            <version>0.11.0</version>
        </dependency>
        <!--操作CSV-->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-csv</artifactId>
            <version>1.10.0</version>
        </dependency>
        <!-- 其他 -->
        <!-- lombok要放在mapstruct前面 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.30</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstructVersion}</version>
        </dependency>
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct-processor</artifactId>
            <version>${mapstructVersion}</version>
        </dependency>

        <!-- Jakarta Annotation API - 用于MapStruct生成Jakarta注解 -->
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <version>2.1.1</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <!-- Maven Compiler 插件 - 配置MapStruct处理器 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.30</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstructVersion}</version>
                        </path>
                    </annotationProcessorPaths>
                    <compilerArgs>
                        <arg>-Amapstruct.unmappedTargetPolicy=IGNORE</arg>
                        <arg>-Amapstruct.defaultComponentModel=spring</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <!-- Maven Antrun 插件 - 替换MapStruct生成的javax为jakarta并重新编译 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>replace-javax-with-jakarta</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <!-- 替换javax.annotation.processing.Generated为jakarta.annotation.Generated -->
                                <replace dir="${project.build.directory}/generated-sources/annotations" token="javax.annotation.processing.Generated" value="jakarta.annotation.Generated">
                                    <include name="**/*.java"/>
                                </replace>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Spring Boot 打包插件 -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!-- JavaFX Maven 插件 - 用于开发阶段运行 -->
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>org.lifxue.wuzhu.WuZhuApplication</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            原生安装包打包（改造，见 docs/plans/DOC_ACCURACY_AUDIT.md §三 / WINDOWS_PACKAGING_WSL2.md §六）：
            原 jpackage-maven-plugin 采用 module 方式（要求 JPMS 模块化）且未绑定执行阶段，与本项目 fat jar
            结构不符、实际不会执行。现改为 <mainJar>（薄 jar WuZhu-1.0.jar.original）+ <mainClass>
            org.lifxue.wuzhu.WuZhuApplication 的 classpath 启动方式（不要用 fat jar 的 JarLauncher，
            见 docs/DEVELOPMENT_RELEASE.md §3.0）。

            使用方式（显式激活，避免与发布流水线冲突）：
              Linux:   ./mvnw clean package -Ppackage-deb   → target/dist/WuZhu-1.0.0.deb
              Windows: .\mvnw.cmd clean package -Ppackage-msi → target\dist\WuZhu-1.0.0.msi

            注意：.github/workflows/release.yml 的发布流程使用 packaging/package-ubuntu.sh 与
            package-windows.ps1 脚本打包（脚本内部会调用 ./mvnw package），因此这里不做按 OS 自动激活，
            否则脚本内的 mvnw package 会重复执行打包。
        -->
        <profile>
            <id>package-deb</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <version>3.6.1</version>
                        <executions>
                            <execution>
                                <id>copy-dependencies-deb</id>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>copy-dependencies</goal>
                                </goals>
                                <configuration>
                                    <!-- includeScope=runtime（compile + runtime）：h2 驱动是 runtime scope，只 copy compile 会漏掉，
                                         导致启动报 Cannot load driver class: org.h2.Driver -->
                                    <includeScope>runtime</includeScope>
                                    <excludeGroupIds>org.openjfx</excludeGroupIds>
                                    <excludeArtifactIds>spring-boot-devtools,spring-boot-configuration-processor</excludeArtifactIds>
                                    <outputDirectory>${project.build.directory}/dependency</outputDirectory>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- 在 verify 阶段复制 fat jar 到 input 目录并在其后执行 jpackage -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>copy-fat-jar-deb</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <!-- 与 package-msi 一致：使用薄 jar（.original），classpath 可加载 -->
                                        <copy file="${project.build.directory}/${project.build.finalName}.jar.original"
                                              tofile="${project.build.directory}/dependency/${project.build.finalName}.jar"/>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.panteleyev</groupId>
                        <artifactId>jpackage-maven-plugin</artifactId>
                        <version>1.7.4</version>
                        <executions>
                            <execution>
                                <id>jpackage-deb</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <name>WuZhu</name>
                                    <appVersion>${project.version}</appVersion>
                                    <vendor>lifxue</vendor>
                                    <description>WuZhu - 加密货币交易记录和分析工具</description>
                                    <mainJar>${project.build.finalName}.jar</mainJar>
                                    <mainClass>org.lifxue.wuzhu.WuZhuApplication</mainClass>
                                    <input>${project.build.directory}/dependency</input>
                                    <destination>${project.build.directory}/dist</destination>
                                    <type>DEB</type>
                                    <icon>${project.basedir}/src/main/resources/org/lifxue/wuzhu/images/logo.png</icon>
                                    <linuxPackageName>wuzhu</linuxPackageName>
                                    <linuxAppCategory>Office</linuxAppCategory>
                                    <linuxMenuGroup>Office</linuxMenuGroup>
                                    <linuxShortcut>true</linuxShortcut>
                                    <javaOptions>
                                        <option>-Dfile.encoding=UTF-8</option>
                                        <!-- 非模块化 classpath 应用需显式加载内置 runtime 的 JavaFX 模块 -->
                                        <option>--add-modules</option>
                                        <option>javafx.base,javafx.graphics,javafx.controls,javafx.fxml,javafx.web,javafx.media,javafx.swing</option>
                                    </javaOptions>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>package-msi</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <version>3.6.1</version>
                        <executions>
                            <execution>
                                <id>copy-dependencies-msi</id>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>copy-dependencies</goal>
                                </goals>
                                <configuration>
                                    <!-- includeScope=runtime（compile + runtime）：h2 驱动是 runtime scope，只 copy compile 会漏掉，
                                         导致启动报 Cannot load driver class: org.h2.Driver -->
                                    <includeScope>runtime</includeScope>
                                    <excludeGroupIds>org.openjfx</excludeGroupIds>
                                    <excludeArtifactIds>spring-boot-devtools,spring-boot-configuration-processor</excludeArtifactIds>
                                    <outputDirectory>${project.build.directory}/dependency</outputDirectory>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>copy-fat-jar-msi</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <!--
                                            使用 Spring Boot 的"薄 jar"（.original = 未 repackage 前的原 jar，
                                            应用类在 jar 根部，可被 jpackage 的 classpath 启动直接加载），
                                            重命名为 ${finalName}.jar 作为 jpackage 的 main jar。
                                            不能用 fat jar：其应用类藏在 BOOT-INF/ 里，classpath 启动无法加载。
                                        -->
                                        <copy file="${project.build.directory}/${project.build.finalName}.jar.original"
                                              tofile="${project.build.directory}/dependency/${project.build.finalName}.jar"/>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.panteleyev</groupId>
                        <artifactId>jpackage-maven-plugin</artifactId>
                        <version>1.7.4</version>
                        <executions>
                            <execution>
                                <id>jpackage-msi</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <name>WuZhu</name>
                                    <appVersion>${project.version}</appVersion>
                                    <vendor>lifxue</vendor>
                                    <description>WuZhu - 加密货币交易记录和分析工具</description>
                                    <mainJar>${project.build.finalName}.jar</mainJar>
                                    <mainClass>org.lifxue.wuzhu.WuZhuApplication</mainClass>
                                    <input>${project.build.directory}/dependency</input>
                                    <destination>${project.build.directory}/dist</destination>
                                    <type>MSI</type>
                                    <icon>${project.basedir}/src/main/resources/org/lifxue/wuzhu/images/wuzhu-96.ico</icon>
                                    <winMenu>true</winMenu>
                                    <winMenuGroup>WuZhu</winMenuGroup>
                                    <winShortcut>true</winShortcut>
                                    <winDirChooser>true</winDirChooser>
                                    <winPerUserInstall>true</winPerUserInstall>
                                    <winUpgradeUuid>a1b2c3d4-e5f6-7890-abcd-ef1234567890</winUpgradeUuid>
                                    <javaOptions>
                                        <option>-Dfile.encoding=UTF-8</option>
                                        <!-- 非模块化 classpath 应用需显式加载内置 runtime 的 JavaFX 模块，否则启动即失败 -->
                                        <option>--add-modules</option>
                                        <option>javafx.base,javafx.graphics,javafx.controls,javafx.fxml,javafx.web,javafx.media,javafx.swing</option>
                                    </javaOptions>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
